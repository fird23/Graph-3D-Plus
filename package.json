{
  "name": "graph-3d-plus",
  "version": "0.1.0",
  "description": "3D graph view with tags, attachments, and overlays.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "obsidian": "^1.5.12",
    "@types/node": "^22.0.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
